	按照他们的readme安装都会稍有问题，所以自己写了脚本，如果搞不起就试试我那个脚本安装。

	两个虽然都调用的gfm官方api，但是渲染出来有点差异，区别是：
	flavor好像是完全遵照gfm规范，用法很像markdown，即命令行执行flavor SRC_FILE > DST_FILE
	gfms专门针对github的readme，也就是渲染出来跟github上的README一摸一样，有外围边框，而且“不认回车，即之前的两行会变成一行”，而且gfms最叼的是，他相当于开了个本地服务端，你浏览器访问本地指定地址就可以看到效果，而且此时你修改文档，浏览器会实时更新（但此时不会得到github的额外渲染，如果需要则刷新页面，例如渲染代码）。

	另外，在公司的网络条件下两者都能用，flavor安装不需要连接外网，gfms安装node需要连外网。
